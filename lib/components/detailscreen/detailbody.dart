import 'package:flutter/material.dart';

class DetailscreenBody extends StatelessWidget {
  const DetailscreenBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
